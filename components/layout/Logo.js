import styled from "styled-components";

const StyledLogo = styled.h3`
  font-size: 1rem;
  color: var(--fg-bold);
`;

const Logo = () => {
  return <StyledLogo>SKD</StyledLogo>;
};

export default Logo;
